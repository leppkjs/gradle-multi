# ===================================================================
# COMMON SPRING BOOT PROPERTIES
#
# This sample file is provided as a guideline. Do NOT copy it in its
# entirety to your own application.               ^^^
# 
# Reference Site : http://docs.spring.io/spring-boot/docs/current/reference/html/common-application-properties.html
#
# ===================================================================

# INTERNATIONALIZATION (MessageSourceAutoConfiguration) ==========
spring:
  messages:
    basename: messages/message-framework, messages/validation, messages/message-web2 
# H2 Web Console (H2ConsoleProperties) ============================
  h2:
    console:
      enabled: true
      path: /h2-console

# DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
  datasource:
    url: jdbc:h2:file:~/test
    username: sa
    password: 
    driver-class-name: org.h2.Driver
    schema: classpath:import.sql


    
# EMBEDDED SERVER CONFIGURATION (ServerProperties) ================
server:
  port: 4242
  context-path: /kai-server

## LOGGING ========================================================
logging:
  config: classpath:logback-spring.xml
  
mybatis:
  config-location: classpath:mybatis-config.xml
  type-aliases-package: com.kcb
  mapper-locations: classpath:mapper/*.xml

